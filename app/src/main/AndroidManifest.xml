<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdoctruyenandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".DoiMatKhauActivity"
            android:theme="@style/AppThemeReg">

        </activity>
        <activity
            android:name=".DoiThongTinActivity"
            android:theme="@style/AppThemeReg" />
        <activity
            android:name=".MainActivity2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChapterScrollingActivity"
            android:label="@string/title_activity_chapter_scrolling"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PageActivity"
            android:theme="@style/AppThemeReg" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".Register"
            android:theme="@style/AppThemeReg" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>